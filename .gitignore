# runtime state (the SQLite ledger + per-run artifacts)
.taskbundle/

# local editor settings
.claude/settings.local.json

# python
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# the hello-task example demonstrates the auto-detect tier, so its Dockerfile is
# generated by `task init`, never committed (committing it would flip it to tier 1)
cli/examples/hello-task/Dockerfile
.DS_Store
